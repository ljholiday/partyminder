<?xml version="1.0"?>
<ruleset name="PartyMinder">
    <description>PHP Code Sniffer configuration for PartyMinder plugin</description>
    
    <!-- Use WordPress Coding Standards -->
    <rule ref="WordPress">
        <!-- Exclude rules that conflict with modern PHP practices -->
        <exclude name="WordPress.Files.FileName.NotHyphenatedLowercase" />
        <exclude name="WordPress.Files.FileName.InvalidClassFileName" />
        <exclude name="Generic.Arrays.DisallowShortArraySyntax" />
        <exclude name="WordPress.PHP.YodaConditions.NotYoda" />
        <exclude name="WordPress.NamingConventions.ValidVariableName.VariableNotSnakeCase" />
    </rule>
    
    <!-- Additional PartyMinder-specific rules -->
    <rule ref="WordPress.Security.EscapeOutput" />
    <rule ref="WordPress.Security.NonceVerification" />
    <rule ref="WordPress.Security.ValidatedSanitizedInput" />
    <rule ref="WordPress.WP.PreparedSQL" />
    
    <!-- Files to check -->
    <file>includes/</file>
    <file>templates/</file>
    <file>partyminder.php</file>
    <file>uninstall.php</file>
    
    <!-- Files to exclude -->
    <exclude-pattern>*/vendor/*</exclude-pattern>
    <exclude-pattern>*/node_modules/*</exclude-pattern>
    <exclude-pattern>*/tests/*</exclude-pattern>
    <exclude-pattern>*/assets/*</exclude-pattern>
    <exclude-pattern>*/.git/*</exclude-pattern>
    
    <!-- Arguments -->
    <arg value="sp"/> <!-- Show sniff and progress -->
    <arg name="basepath" value="./"/>
    <arg name="colors"/>
    <arg name="extensions" value="php"/>
    <arg name="parallel" value="8"/>
    
    <!-- PHP version compatibility -->
    <config name="testVersion" value="7.4-"/>
</ruleset>